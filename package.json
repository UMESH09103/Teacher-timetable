{
  "name": "school-timetable-system",
  "version": "1.0.0",
  "description": "School Smart Timetable & Substitute Teacher Management System",
  "scripts": {
    "start": "node backend/server.js",
    "build": "npm --prefix backend install && npm --prefix frontend install && npm --prefix frontend run build",
    "render-build": "npm --prefix backend install && npm --prefix frontend install && npm --prefix frontend run build",
    "seed": "node backend/seed/seed.js",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}